# AGENTS.md — LotoVN AI

Hướng dẫn cho mọi coding agent làm việc trong repo này (Antigravity, Claude Code, Cursor, Codex, v.v.). Đọc file này trước khi sửa code.

## Dự án là gì

LotoVN AI — ứng dụng thống kê minh bạch dữ liệu & giáo dục xác suất cho xổ số Vietlott (Mega 6/45, Power 6/55). Bài dự thi **AI Riser Vietnam 2026 "Builder"** (Google), mục tiêu Top 500 (Silver Tier).

## Ràng buộc quan trọng nhất — đọc trước khi làm bất cứ gì

Luật cuộc thi yêu cầu ứng dụng phải được phát triển chủ đạo bằng **Google AI Studio**, và bài nộp bắt buộc kèm AI Studio project link công khai để giám khảo xem lịch sử build/prompt thật.

- Vai trò của các coding agent khác (Antigravity, Claude Code, Cursor...) trong dự án này là **hỗ trợ**, KHÔNG thay thế AI Studio: fix bug, refactor phức tạp, tác vụ tốn nhiều bước.
- Sau mỗi đợt sửa, code PHẢI được push lên GitHub rồi import lại vào AI Studio để lịch sử build phản ánh đúng bản mới nhất. Vì vậy:
  - Ưu tiên các thay đổi **nhỏ, có mục tiêu rõ ràng, dễ giải thích** trong một prompt tiếp theo trên AI Studio.
  - Nếu cần đổi kiến trúc lớn (ví dụ: tách trang, đổi routing), hãy làm theo từng bước rõ ràng, mỗi bước là một trạng thái chạy được (không để dở dang giữa chừng), và giải thích trong commit message vì sao đổi.
- Không tự ý thêm/đổi thư viện UI runtime hay đổi package manager mà không hỏi trước.

## Tech stack đã chốt (không tự ý đổi)

- **Next.js (App Router) + TypeScript**
- **Tailwind CSS thuần** — KHÔNG dùng HeroUI hay UI runtime library khác. Project đã có nền tảng kiểu shadcn/ui (`class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`) — ưu tiên pattern này khi cần thêm component mới.
- **MongoDB** qua `MONGODB_URI`, có fallback in-memory seed data khi không có kết nối hoặc kết nối lỗi (xem `lib/db.ts`). PHẢI giữ hành vi fallback này trong mọi refactor — đây là lý do app luôn chạy được kể cả khi chưa cấu hình secret.
- **Firebase Authentication** (Email/Password + Google Sign-In) — chỉ dùng Auth, KHÔNG dùng Firestore/Storage/Cloud Functions.
- **Gemini API** chỉ gọi ở server-side (route handlers / server components, qua `lib/gemini.ts`) — không bao giờ để lộ `GEMINI_API_KEY` ra client.
- **Recharts** cho biểu đồ — luôn đi qua wrapper `components/ui/chart.tsx` (shadcn `ChartContainer`/`ChartTooltip`/`ChartTooltipContent`/`ChartConfig`), KHÔNG dùng `<Tooltip contentStyle={{...}}>` hay màu hex hardcode trực tiếp trên `<Cell>` nữa — dùng `var(--color-chart-1)` .. `var(--color-chart-5)` (định nghĩa trong `app/globals.css`) để tự động đúng theme.
- `app/globals.css` có block `:root, .dark { --background: ...; --popover: ...; ... }` + `@theme inline` map sang `--color-*` — đây là điều kiện bắt buộc để mọi component shadcn (Dialog/Select/Chart...) hiển thị đúng màu. Nếu thêm component shadcn mới mà thấy nó trong suốt/sai màu, kiểm tra file này trước, đừng chỉ override từng className thủ công.
- Package manager: dev local dùng **pnpm** (`pnpm-lock.yaml`). Repo còn `bun.lock` từ lúc AI Studio khởi tạo project — đang giữ lại cả 2 lockfile, `npx shadcn add`/các CLI khác có thể tự nhận nhầm sang `bun` nếu `bun` có trong PATH; nếu vậy dùng `pnpm` tường minh (`pnpm add ...`) thay vì để CLI tự đoán.

## Định vị sản phẩm — bám sát khi sửa nội dung/copy

Triết lý cốt lõi: **minh bạch dữ liệu và giáo dục xác suất**, KHÔNG phải dự đoán/may mắn/tâm linh.

- Tránh hoàn toàn yếu tố phong thủy, hoàng đạo, "may mắn" — đây là hướng đi phổ biến của đối thủ, LotoVN AI cố tình khác biệt.
- Tính năng tracking độ chính xác gợi ý phải trung thực — tỷ lệ khớp số thấp là bình thường và nên được thể hiện rõ, không "làm đẹp" số liệu.
- Thiết kế: dark theme "Sophisticated Dark" — nền slate/zinc trung tính, accent teal/indigo. Tránh đỏ/vàng kiểu "lucky charm".
- Toàn bộ copy UI bằng **tiếng Việt**; code, biến, comment bằng **tiếng Anh**.

## Quy tắc code

- Không thêm comment giải thích điều hiển nhiên; chỉ viết comment khi có lý do ẩn (constraint, workaround) thật sự cần giải thích, và viết bằng tiếng Anh.
- Không thêm abstraction, tính năng, hay "cải thiện" ngoài phạm vi task được giao.
- Refactor không được làm vỡ giao diện/hành vi hiện có trừ khi task yêu cầu rõ ràng — so sánh trước/sau bằng mắt (`npm run dev`) trước khi báo hoàn thành.
- Trước khi báo xong một task: chạy `npx tsc --noEmit` (không lỗi type) và tự chạy `npm run dev` để kiểm tra UI thật, đặc biệt ở mobile/tablet/desktop breakpoint.
- Không commit `.env*` (trừ `.env.example`), không commit build artifact (`.next/`, `*.tsbuildinfo`, `node_modules/`).
- Không để lại bất kỳ dấu hiệu "Generated by AI" / tên công cụ AI nào trong code, commit message, hay UI hiển thị cho người dùng.
- Ưu tiên sửa file có sẵn hơn tạo file mới; chỉ tách component mới khi file gốc thực sự quá lớn/đa trách nhiệm.

## Streaming SSR — quy ước bắt buộc cho Server Component fetch data

Dashboard (`/`) và Suggestions (`/suggestions`) dùng streaming SSR: mỗi khối UI là một async Server Component tự fetch phần dữ liệu của mình, không nhận data qua props từ page cha (xem `components/dashboard/`, `components/suggestions/`).

- **Skeleton + Suspense phải khai báo VÀ dùng ngay trong cùng file component**, không tách skeleton ra file riêng rồi import vào `page.tsx` để tự ráp `<Suspense>`. Pattern chuẩn:
  ```tsx
  function XSkeleton() { ... }               // không export, chỉ dùng nội bộ
  async function XContent(props) { ... }      // phần fetch + render thật
  export function X(props) {                  // export duy nhất, page.tsx chỉ gọi <X {...props} />
    return <Suspense fallback={<XSkeleton />}><XContent {...props} /></Suspense>;
  }
  ```
- Mọi hàm fetch dùng chung trong nhiều Server Component ở cùng 1 trang (`getLotteryStats`, `getDraws`, `getSuggestionLogs`) PHẢI bọc `cache()` từ `react` trong `lib/db.ts` — tránh query MongoDB lặp lại khi nhiều Suspense boundary cùng gọi trong 1 request.
- State điều khiển việc fetch lại dữ liệu (khoảng thời gian, bộ lọc...) đi qua URL search param (`?range=`, `?filter=`, `?type=`), KHÔNG dùng React state + fetch client-side — để tận dụng streaming/Suspense khi đổi giá trị.

## Enum & hằng số — tránh magic number/string

- Domain value có tập giá trị cố định, nhỏ, được validate từ query param (ví dụ `TimeRange`, `SuggestionFilter` trong `lib/types.ts`) → khai báo `enum`, không dùng number/string trần rải rác trong code.
- Ngưỡng so sánh lặp lại nhiều nơi (ví dụ `GAN_THRESHOLD` trong `lib/types.ts`) → đặt thành hằng số named export, không hardcode lại số.
- **Ngoại lệ có chủ đích**: `LotteryType` KHÔNG chuyển sang `enum` — TypeScript string-literal union đã type-safe tương đương, được dùng làm giá trị lưu trực tiếp trong MongoDB/API, và chuyển sang enum sẽ đụng ~90 nơi so sánh chuỗi hiện có mà không thêm lợi ích nào. Đừng "sửa" lại thành enum.

## JSDoc bắt buộc cho mọi component

Mỗi component export (kể cả các hàm `Content`/`Client` nội bộ không export) cần 1 đoạn JSDoc ngắn (1-4 dòng) ngay trên khai báo, mô tả component làm gì và lưu ý quan trọng (ví dụ: vì sao là async Server Component, vì sao tách Client riêng). Không viết JSDoc dài dòng liệt kê từng prop — tên prop + TypeScript interface đã đủ tự giải thích.

## Cron job đồng bộ kỳ quay (`app/api/cron/sync-draws`)

Vietlott KHÔNG có API công khai chính thức — trang kết quả là ASP.NET WebForms cũ, tải bảng qua RPC nội bộ "AjaxPro" (không phải REST/JSON thông thường). `lib/vietlott-source.ts` xử lý việc này theo 2 tầng:

1. **Nguồn chính** — gọi thẳng endpoint AjaxPro của vietlott.vn (`OFFICIAL_ENDPOINTS`), parse HTML trả về bằng `cheerio`. Request body/header tham khảo từ mã nguồn mở MIT license [vietvudanh/vietlott-data](https://github.com/vietvudanh/vietlott-data), đã tự verify khớp 100% với trang chính thức.
2. **Nguồn dự phòng** — nếu nguồn chính lỗi (`try/catch` tự động), fallback sang file `.jsonl` của cùng repo trên (cập nhật ~00:01 hàng ngày).

**Lưu ý quan trọng đã verify thực tế**: gọi nguồn chính từ Cloud Run (`asia-southeast1`) bị Vietlott trả về **HTTP 403** — nhiều khả năng họ chặn dải IP của các nhà cung cấp cloud lớn để chống bot. Từ máy cá nhân/local dev thì gọi được bình thường. Vì vậy trong production hệ thống sẽ chủ yếu chạy qua nguồn dự phòng (đã verify dữ liệu khớp chính xác với nguồn chính) — đây là giới hạn hạ tầng thực tế, KHÔNG cố gắng "qua mặt" chặn IP (đổi User-Agent, dùng proxy...) — không phù hợp về mặt đạo đức/ToS cho một bài dự thi.

**Bảo mật & vận hành**:
- Endpoint chấp nhận 2 cách xác thực qua header `Authorization: Bearer <token>`: `CRON_SECRET` (Secret Manager, secret `cron-secret`) cho Cloud Scheduler, HOẶC Firebase ID token của admin cho nút "Chạy Đồng Bộ Ngay" ở `/admin` (xem `lib/admin-auth.ts`).
- Idempotent — so sánh `id` với draw đã có trong DB trước khi insert, gọi lại nhiều lần trong ngày không tạo bản ghi trùng.
- Lỗi ở 1 loại vé số (mega645/power655) KHÔNG chặn loại còn lại — mỗi loại được try/catch riêng, lỗi được ghi vào `results[type].error` thay vì abort cả request.
- Mọi lần chạy (kể cả lỗi một phần) đều được ghi vào collection `cron_logs` qua `saveCronLog`/`getCronLogs` (`lib/db.ts`) — hiển thị trên `/admin`.
- Kích hoạt tự động bởi Cloud Scheduler job `vietlott-sync-draws-daily` (region `asia-southeast1`), chạy `0 1 * * *` giờ `Asia/Ho_Chi_Minh` — chạy hàng ngày (không cố khớp đúng lịch quay Mega/Power riêng biệt) vì logic idempotent nên chạy thừa không hại gì.

## Trang Admin (`/admin`)

Dashboard nội bộ xem lịch sử đồng bộ (`cron_logs`) + nút chạy đồng bộ thủ công — chỉ 1-2 admin dùng, KHÔNG phải tính năng cho người dùng thường.

- **Route group**: `/admin` cố tình nằm NGOÀI `app/(main)/` (route group chứa `/`, `/suggestions`, `/chat`, `/education` + `Navbar`/`Footer` dùng chung, xem `app/(main)/layout.tsx`). `app/layout.tsx` (root thật) chỉ còn `<html>/<body>` + `AppProviders`, không có Navbar/Footer — lý do: `/admin` không được có link/chrome trỏ qua lại với phần công khai, và Navbar phụ thuộc `usePathname()` nên nếu dùng chung layout sẽ bị hydration mismatch khi `/admin` redirect client-side ra `/`.
- **Gate 2 lớp**: client (`components/admin/AdminDashboard.tsx`) chỉ quyết định UI — dùng `authLoading` từ `AppProviders` để đợi Firebase xác định xong trạng thái đăng nhập trước khi quyết định redirect (tránh redirect nhầm admin thật do race condition lúc `user` còn `null` tạm thời). Redirect thẳng về `/` nếu chưa đăng nhập hoặc không phải admin — KHÔNG hiện trang "không có quyền" tĩnh. Gate thật (bảo mật thật sự) nằm ở SERVER — mọi API `/api/admin/*` và `/api/cron/sync-draws` đều verify Firebase ID token qua `lib/admin-auth.ts` (`verifyIdToken` + so email với `ADMIN_EMAILS`), không dựa vào việc client có ẩn UI hay không.
- `ADMIN_EMAILS`: server-only env var, danh sách email phân cách dấu phẩy. KHÔNG dùng `NEXT_PUBLIC_*` (sẽ lộ danh sách admin trong client bundle).
- `firebase-admin` cần Application Default Credentials để verify ID token: trên Cloud Run tự động dùng service account đã gắn sẵn; ở local dev chạy 1 lần `gcloud auth application-default login` rồi `gcloud auth application-default set-quota-project lotovn-ai` (quota project mặc định có thể trỏ nhầm sang project gcloud khác đang active — đã tự gặp lỗi 403 `identitytoolkit.googleapis.com` vì việc này).

## Firebase Auth — Google Sign-In & account linking

- **Authorized Domains**: domain Cloud Run live (`lotovn-ai-308915299258.asia-southeast1.run.app`) phải nằm trong `authorizedDomains` của Identity Platform config, nếu không Google Sign-In popup lỗi `auth/unauthorized-domain`. Kiểm tra/sửa qua Identity Toolkit Admin API (`identitytoolkit.googleapis.com/admin/v2/projects/{project}/config`, cần header `x-goog-user-project`), không phải setting của riêng Firebase Console mà CLI thường không đụng tới. Nếu đổi domain Cloud Run (custom domain, region khác...), nhớ cập nhật lại danh sách này.
- **Dùng `signInWithRedirect`, KHÔNG dùng `signInWithPopup`** — popup gặp lỗi IndexedDB thoáng qua (`InvalidStateError` / "database connection is closing", bug lâu năm chưa Firebase fix chính thức: [firebase-js-sdk#2710](https://github.com/firebase/firebase-js-sdk/issues/2710)) khi tab chính mất focus lúc popup Google mở/đóng, gặp thật ở local dev nhiều lần liên tiếp — không phải hiện tượng hiếm. `loginWithGoogleRedirect()` (`lib/firebase.ts`) điều hướng cả trang sang Google, không mở popup. Kết quả được đọc lại 1 lần khi app khởi động qua `consumeGoogleRedirectResult()` (gọi trong `useEffect` ở `AppProviders.tsx`, tách riêng khỏi `onAuthStateChanged`) — nếu không có redirect nào đang chờ thì trả về `null`, không phải lỗi.
- **1 tài khoản / 1 email** (`allowDuplicateEmails` không bật) — nếu 1 email đăng ký Email/Password rồi sau đó "Đăng nhập Google" cùng email, Firebase từ chối bằng `auth/account-exists-with-different-credential` thay vì tự gộp. Vì dùng redirect, lỗi này xuất hiện khi gọi `consumeGoogleRedirectResult()` (không phải ngay khi bấm nút như popup) — `AppProviders.tsx` bắt `GoogleAccountLinkRequiredError`, lưu `pendingGoogleLink` vào context rồi tự mở `AuthModal` với form đăng nhập mật khẩu kèm hướng dẫn; `AuthModal` gọi `linkPendingGoogleCredential` sau khi đăng nhập thành công để liên kết 2 phương thức vào cùng 1 tài khoản. Chiều ngược lại (đăng ký Email/Password khi email đã tồn tại qua Google) chỉ có thông báo hướng dẫn (`auth/email-already-in-use`), CHƯA có auto-link ngược.
- `TransientStorageError` (IndexedDB lỗi thoáng qua) vẫn có thể xảy ra ở bước `consumeGoogleRedirectResult()` (đọc session từ IndexedDB) dù ít hơn hẳn so với popup — bắt lỗi này thì hiện toast và không tự reload cưỡng bức (khác với trước đây), vì lúc này không còn ở giữa luồng UI tương tác trực tiếp như popup nữa.
- **ĐỪNG dùng `initializeAuth(app, { persistence: [...] })` (mảng nhiều persistence)** — đã thử và gây regression thật: ném `auth/argument-error` ngay khi bấm nút. TypeScript type `Dependencies.persistence` cho phép `Persistence | Persistence[]` nên `tsc` không báo lỗi, nhưng runtime của `@firebase/auth` bản đang dùng (`^12.17.0` / `@firebase/auth@1.13.4`) không chạy đúng với dạng mảng. Giữ nguyên `getAuth(app)` đơn giản.

## MongoDB — 1 collection dùng chung cho Mega/Power, không tách

`draws`/`suggestions` lưu chung cho cả `mega645` và `power655`, phân biệt bằng field `lotteryType` — đây là thiết kế đúng chuẩn MongoDB (schema giống hệt nhau, dữ liệu nhỏ), KHÔNG tách thành 2 collection riêng (sẽ phải nhân đôi logic query mà không lợi gì ở quy mô này). Đã có compound index `{lotteryType: 1, drawDate: -1}` (draws), `{lotteryType: 1, createdAt: -1}` (suggestions), `{runAt: -1}` (cron_logs) — tạo qua `ensureIndexes()` trong `lib/db.ts`, gọi mỗi lần connect mới (idempotent, `createIndex` tự no-op nếu đã tồn tại).

## Lỗi đã biết — đừng tái tạo lại

- **`grid-cols-15` (và các số cột/hàng grid bất kỳ) là hợp lệ trong Tailwind v4** — v4 sinh utility `grid-cols-N`/`grid-rows-N` động cho mọi số nguyên dương, không giới hạn ở thang 1-12 như v3. Đã verify bằng `@tailwindcss/cli` thực tế: `grid-cols-15` sinh đúng `grid-template-columns: repeat(15, minmax(0, 1fr))`. KHÔNG đổi thành `grid-cols-[repeat(15,minmax(0,1fr))]` — đó là dạng thừa, không cần thiết trong project này (đã từng bị "sửa" sai 3 lần trong lịch sử, gây nhầm lẫn — giữ nguyên dạng `grid-cols-15` trong `MonteCarloSimulator.tsx`).
